### 2023-2023 第二学期编译原理期末考试（回忆版）
#### 简答
1. 同2021-2022 B卷 1
2. 同2021-2022 B卷 2
3. 为什么要生成中间代码，而不是直接生成目标代码？
4. 在可用表达式中，为什么除了 entry，其它基本块的 out 初始化为全集？为什么可用表达式的传递取交集？

#### 问答
1. 写出不包含子串 `11` 的所有串的正则表达式（字母表：$\Sigma=\{0,1\}$）
2. 写出接受回文串的正则定义（字母表：$\Sigma=\{0,1\}$）这是个错题！！！
3. 画出接受所有十进制下为3的倍数的二进制串（不包含空串）的 NFA（NFA 状态不超过4个，转移边不超过10个）
4. 计算给定文法的 LR(0) 项集规范族 
5. 同2021-2022 B 卷 二、7（设计语义规则）
6. 根据给定文法规则写出 `x = a[b[i]][c[j][k]]` 的三地址码。
7. 对给定 ir ，画出 DAG 并对其进行常量传播、公共表达式消除、死代码消除（O2）
8. 对给定 CFG，计算其到达定值的 IN，OUT。
9. 对给定的有向图，写出其引用计数垃圾回收和标记-清扫垃圾回收后被释放的内存块。
#### 填空题
1. 解释静态单赋值。
2. 为什么语法分析不能识别语义错误？
3. 布尔表达式的控制流翻译和回填（题干不记得了）